# Local Preview

From this folder, run:

```powershell
python -m http.server 8000
```

Then open:

```text
http://localhost:8000
```

You can also double-click `index.html`, but using a local server is closer to how GitHub Pages will serve the site.
