# Release Notes

## v3, flat GitHub Pages package

- Removed the dependency on an `assets/` folder to reduce GitHub browser upload mistakes.
- Moved `logo.svg`, `favicon.svg`, and `social-card.svg` to the repository root.
- Updated `index.html` and `404.html` to reference the root-level SVG files.
- Kept the site as plain static HTML, CSS, and JavaScript.
